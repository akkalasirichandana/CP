#include <iostream>
#include <string>
using namespace std;
int main() {
    string s;
    cin >> s;
    int seen = 0;
    int duplicate = 0;
    bool found = false;
    for (char c : s) {
        int bit = c - 'a';
        if (seen & (1 << bit)) {
            duplicate |= (1 << bit);
        } else {
            seen |= (1 << bit);
        }
    }
    for (char c : s) {
        int bit = c - 'a';
        if (duplicate & (1 << bit)) {
            cout << c << " ";
            duplicate &= ~(1 << bit);  
            found = true;
        }
    }
    if (!found) {
        cout << "No duplicates";
    }
    return 0;
}
